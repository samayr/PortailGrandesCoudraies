var Authentication = (function () {
    function Authentication() {
    }
    Authentication.prototype.Initialize = function () {
    };

    Authentication.prototype.SaveCookie = function () {
    };

    Authentication.prototype.GetCookie = function () {
    };

    Authentication.prototype.AuthenticateByCookie = function () {
    };

    Authentication.prototype.AuthenticateByPassword = function (pLogin, pPassword) {
    };
    return Authentication;
})();
//# sourceMappingURL=Authentication.js.map
